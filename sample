  if (element.attr('data-element-type') === 'date') {
                    error.appendTo(element.closest('.form-group')); // Place error message outside the div with class 'input-group date'
                } else {
                    error.insertAfter(element); // Default placement for other elements
                }
