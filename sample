var $container = $('.error-message[data-for="' + element.attr("name") + '"]');
                if (error.text() !== '') {
                    $container.show().text(error.text()); // Show error message and set text
                } else {
                    $container.hide().empty(); // Hide error message and clear text if validation passes
                }
