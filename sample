get radioOptions(): { value: string; label: string }[] {
    return Array.from({ length: this.numberOfOptions }, (_, i) => ({
      value: `Option${i + 1}`,
      label: `Option ${i + 1}`
    }));
  }
